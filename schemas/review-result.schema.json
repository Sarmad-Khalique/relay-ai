{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://relay-ai.dev/schemas/review-result.schema.json",
  "title": "ReviewResult",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema_version", "task_id", "verdict", "summary", "findings"],
  "properties": {
    "schema_version": { "const": "1.0" },
    "task_id": { "type": "string", "minLength": 1 },
    "verdict": { "enum": ["accepted", "changes_requested"] },
    "summary": { "type": "string" },
    "findings": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["id", "severity", "title", "evidence", "required_change"],
        "properties": {
          "id": { "type": "string", "minLength": 1 },
          "severity": { "enum": ["blocking", "advisory"] },
          "path": { "type": "string" },
          "line": { "type": "integer", "minimum": 1 },
          "title": { "type": "string", "minLength": 1 },
          "evidence": { "type": "string", "minLength": 1 },
          "required_change": { "type": "string" }
        }
      }
    }
  }
}
